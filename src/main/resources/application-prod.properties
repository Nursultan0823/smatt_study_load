# # Продакшен (VPS и т.п.): порт и CORS как на сервере
# server.port=${SERVER_PORT:8034}
# spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/smartsudyload}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:crascot}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root}
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false
# app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://95.217.88.31:8033,http://localhost:3000}


# Render передаёт порт через переменную PORT.
server.port=${PORT:10000}
server.address=0.0.0.0

# Database
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

spring.datasource.hikari.maximum-pool-size=${DB_POOL_MAX_SIZE:5}
spring.datasource.hikari.minimum-idle=${DB_POOL_MIN_IDLE:0}
spring.datasource.hikari.connection-timeout=${DB_CONNECTION_TIMEOUT:30000}

# Hibernate
spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS}